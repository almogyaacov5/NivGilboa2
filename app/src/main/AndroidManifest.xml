<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nivgilboaapp">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NIVAPP"
        tools:targetApi="31">

        <!-- Activities -->
        <activity android:name=".YouTubeSearchActivity" />

        <!-- הוספתי את מסך הסינון כדי למנוע קריסה בלחיצה על כפתור הסינון -->
        <activity android:name=".FilterActivity" android:exported="false" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAaQeMDU7bBKWVZZIAp-628Ymixzz4q-Fc" />

        <!-- Main Activity -->
        <activity
            android:name="com.example.nivgilboaapp.MainActivity"
            android:exported="true"
            android:label=""> <!-- השינוי כאן: מוחק את הכותרת מהפס העליון במסך הזה בלבד -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.nivgilboaapp.AddRestaurantActivity"
            android:exported="false" />

        <activity
            android:name="com.example.nivgilboaapp.RestaurantDetailActivity"
            android:exported="false" />

    </application>

</manifest>
